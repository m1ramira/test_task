Решение тестового задания

1) Автоматизировать загрузку в БД
2) Вывести данные из БД в таблицу

Для реализации п.1 был создан скрипт json_parser.py, который получает данные из 2-х предоставленных json и преобразует их в новый json,
который имеет вид 
[
  {
   'model': my_app.My_model,
   'pk': 1,
   'fields': {'my_fields_1': ..., 'my_fields_2': ...}
  }
]
Полученные данный сохраняются в db.json
Далее с помощью команды python manage.py loaddata db.json данные загружаются в БД

П.2 был реализован с помощью функции post_list (blod/view.py), создан шаблон table.html, добавлена таблица стилей blog.css

